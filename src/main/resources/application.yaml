server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_manager
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
